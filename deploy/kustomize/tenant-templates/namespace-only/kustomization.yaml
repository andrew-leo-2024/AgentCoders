apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Template for namespace-only isolation tier
# Variables TENANT_NAMESPACE, TENANT_ID are substituted at provisioning time

resources:
  - namespace.yaml
  - network-policy.yaml
  - resource-quota.yaml
  - rbac.yaml
