apiVersion: v1
kind: ResourceQuota
metadata:
  name: tenant-quota
  namespace: TENANT_NAMESPACE
spec:
  hard:
    pods: "20"
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "16"
    limits.memory: 32Gi
    persistentvolumeclaims: "5"
    requests.storage: 50Gi
