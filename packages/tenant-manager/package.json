{
  "name": "@agentcoders/tenant-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/tenant-api.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/tenant-api.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@agentcoders/shared": "workspace:*",
    "drizzle-orm": "^0.38.0",
    "ioredis": "^5.4.0",
    "@kubernetes/client-node": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
